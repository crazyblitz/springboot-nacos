

spring:
  cloud:
    nacos:
      config:
        server-addr: 139.199.133.171:8848
        ext-config[0]:
          dataId: nacos_provider_config.yaml
          group: nacos_provider
          refresh: true
        ext-config[1]:
          dataId: nacos_provider_config_dev.properties
          group: nacos_provider
          refresh: true
        file-extension: yaml
        prefix: nacos_provider_config
        group: nacos_provider


